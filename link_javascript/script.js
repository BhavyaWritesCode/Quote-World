// main.js
import { initQuoteGenerator } from './quoteGenerator.js';


// Initialize the quote generator when the DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
  initQuoteGenerator();
});
